/**
 * Maximum width (in px) of the app's content column on phones. The UI is
 * mobile-first, so on narrow viewports content fills the screen up to this cap.
 */
export const APP_MAX_WIDTH = 600

/**
 * Maximum width (in px) of the content column on desktop, where a side
 * navigation rail replaces the bottom nav and the dashboard uses the extra
 * room for multi-column layouts.
 */
export const DESKTOP_MAX_WIDTH = 960

/** Width (in px) of the persistent desktop side navigation rail. */
export const SIDE_NAV_WIDTH = 240
