import { Box, Typography } from '@mui/material'

/** Placeholder Settings page. Replaced by feature agents with full implementation. */
export function SettingsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="titleLarge">Settings</Typography>
    </Box>
  )
}
