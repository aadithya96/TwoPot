import { Box, Typography } from '@mui/material'

/** Placeholder Expenses page. Replaced by feature agents with full implementation. */
export function ExpensesPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="titleLarge">Expenses</Typography>
    </Box>
  )
}
