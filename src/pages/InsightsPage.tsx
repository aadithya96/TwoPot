import { Box, Typography } from '@mui/material'

/** Placeholder Insights page. Replaced by feature agents with full implementation. */
export function InsightsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="titleLarge">Insights</Typography>
    </Box>
  )
}
