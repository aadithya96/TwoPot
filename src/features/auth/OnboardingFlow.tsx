import { Box, Typography } from '@mui/material'

/** Placeholder onboarding flow. Replaced by Agent C with the full multi-step wizard. */
export function OnboardingFlow() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="titleLarge">Onboarding</Typography>
    </Box>
  )
}
