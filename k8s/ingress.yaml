apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: twopot
  namespace: twopot
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - ad-labs.duckdns.org
      secretName: twopot-tls
  rules:
    - host: ad-labs.duckdns.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: twopot
                port:
                  number: 80
